# Salt Basin Master Build — Progress Tracker

Mutable state file for the `salt-basin-master-build` skill (`.claude/skills/salt-basin-master-build/`). Every invocation of `/salt-basin-build` reads this file to decide what's next and writes back what it did. See `.claude/skills/salt-basin-master-build/reference/loops.md` for the static definition of each loop.

## Concurrent-session notice (read before acting)

As of 2026-07-12, multiple Claude Code sessions are actively working this exact scope in parallel on this repo (confirmed: `.claude/skills/salt-basin-master-build/SKILL.md` and `reference/loops.md` were edited mid-turn by another session while this tracker was being written; `server/db.js`, `server/index.js`, `server/routes/journeyRods.js`, and `docs/salt-basin-foundation-source-of-truth.md` are all sitting with live uncommitted changes). Two sibling skills already exist from that concurrent work:

- `.claude/skills/salt-basin-pre-build/` + `docs/salt-basin-pre-build-progress.md` — a 6-phase execution tracker built specifically for the Member/Org companion spec (`member-org-admin-config.md`). Loops 2/3/6/7/16 below defer to its phase statuses rather than duplicating them.
- `.claude/skills/salt-basin-config-audit/` — a configuration-enforcement audit now wired into every loop's step 4 per the concurrently-edited `SKILL.md`.

There is also a **direct-execution track outside any skill**: `context-reconciliation/` (`final-coverage-report.md`, `uncovered-context-register.json`, `architecture-inventory.json`) and `migrations/` (`terminology-migration-registry.json`, `legacy-object-migration-report.md`, `migrated-data-summary.json`) — real Foundation/terminology reconciliation has already happened there. `migrated-data-summary.json` confirms `"liveDatabaseRecordsMutated": 0` — the semantic reclassification pass (Journey Branch → Journey Tributary, etc.) was documentation/registry-level only; no live Postgres rows have been touched anywhere in this effort so far. Treat `context-reconciliation/uncovered-context-register.json` (gaps GAP-001 through GAP-006) as the authoritative "what's structurally still missing" list — don't re-derive a competing one here.

Before running any loop that touches a file another session has open, re-check `git status` fresh — don't trust this tracker's file-path references as proof a file is untouched.

## Flagged conflicts requiring a Betsy decision (not decided here)

- **Live backend Channel-formation rods ignore lead-source genesis rules — Loop 4.** The persisted `journey_data_rods` bootstrap backfill (`server/db.js`) and `server/lib/journeyRods.js` always create `member` + `customer` + `revenue_lifecycle` rods for every real member, unconditionally — no equivalent to the prototype engine's `LEAD_GENESIS_RULES` (external-intake org-buyer vs. direct-consumer vs. internal-sourced) exists there. This directly contradicts §25's DTC rule ("do not force duplicate Customer records solely to support reporting") for real members whose actual lead source was direct-to-consumer. Needs a decision: is the unconditional three-rod backfill an intentional simplification for the live backend (distinct from the prototype's more granular rules), or should real genesis-rule gating eventually apply to new member rows going forward? Not resolved here — touches live bootstrap SQL over real member rows, out of scope for a silent fix.

## Flagged items — decided, not blocking (per Betsy 2026-07-12: "don't worry about changing the conflicts, just reconcile and run")

These were previously flagged as open conflicts requiring a decision before proceeding. Betsy has said to proceed rather than block — decisions below, recorded so future loop runs don't re-litigate them:

- **Channel vs. Channel Rod — RESOLVED 2026-07-16 (Betsy): "Channel will define the definition, Channel Rod reflects the persistent instance of the Channel."** This refines (does not overturn) the existing governing chain — Channel and Channel Rod remain two distinct entities, contra `Salt_Basin_Workbook_001A_Enterprise_Foundation_v4.xlsx`'s `Channel_Model_Migration` sheet, which claimed the rod collapses into the Channel with "no separate rod entity remaining" (that workbook's own `Validation_Checklist` row VAL-001 flagged this as "Not Reviewed" — now reviewed and rejected in favor of the definition/instance framing). Verified the shipped schema already implements this split without having named it: `journey_rod_types` (`server/db.js:556`, real registry table — id/label/description/is_active/sort_order) is the Channel definition; `journey_data_rods` rows are Channel Rod instances of a `rod_type`. Updated `docs/salt-basin-foundation-source-of-truth.md` §11 and `docs/salt-basin-foundation-source-of-truth.registry.json`'s `concept-channel`/`concept-channel-rod` elements to state the definition/instance relationship explicitly and cite their real backing tables. No schema or code change — pure terminology/documentation reconciliation, nothing to regression-gate (no runtime surface changed).
- **Journey vs. Channel — RESOLVED, already decided.** `docs/salt-basin-foundation-source-of-truth.registry.json` already has `canonicalArchitectureTerm: "Channel"` with a full legacy-alias map (`Channel: [Journey]`, `ChannelRod: [JourneyRod, JourneyDataRod]`, etc.) and a `terminology-migration-registry.json` with per-object migration rules. Channel is canonical; Journey is a preserved legacy alias, not a term to delete. Live code (`journeyEngine/*`, `journeyRods.js`) has not yet been renamed at the file/route level — that's still open, additive work (add Channel-named aliases/routes alongside the existing Journey ones; do not drop or rename the underlying tables), but the terminology *decision* itself is closed.
- **Model complexity mismatch — RESOLVED, already reconciled.** The same registry's `governingChain` already unifies master-prompt §5's world model with the 3-tier Atom/Joint/Molecule vocabulary and the 9-layer EIDOS schema into one chain: `Basin → Port → SourceObservation → Element → EvidenceAtom → SemanticAffinityField → AtomCluster → SemanticComposition → ProjectionRelativeSettlement → Channel → ChannelRod → ChannelTributary → Confluence → QueryChannel → CompressedQueryRod → AccountingProjection → ReciprocalStateInference → HeterosemanticDivergence → AgentBoundary → ChannelRodStaff`. No fourth parallel model — this chain is the one.
- **Member/Org authority-model granularity — DECIDED: default to extending `data_entitlements.scope`.** Not the three-array (`authorityProfileIds`/`moduleAccessProfileIds`/`viewAccessProfileIds`) split, per the reconciliation memo's own risk assessment (lower-risk, matches shipped schema, matches the pre-existing crystal-nav spec's mechanism). If a concrete module later needs finer gating than `data_entitlements.scope` JSONB can express, add fields to that JSONB shape first before introducing new tables. Note: `docs/salt-basin-pre-build-progress.md`'s own open-decision table still lists this as "unanswered" as of last read — that tracker should be updated to match, or explicitly told this decision came from a different session, to avoid the two trackers diverging.
- **Two visibility enums — DECIDED: one canonical enum.** The six-value Data Scope enum (`MEMBER_PRIVATE / MEMBER_PUBLIC / ORGANIZATION_PRIVATE / ORGANIZATION_SHARED / CROSS_ORGANIZATION_AUTHORIZED / PUBLIC`, `member-org-admin-config.md` §15) is canonical for both record-level and semantic-object-level visibility. The narrower five-value enum in §3 (`PRIVATE / MEMBER_ONLY / ORGANIZATION_VISIBLE / SHARED_BY_LINK / PUBLIC`) is retired as a separate taxonomy; treat it as UI-copy shorthand mapping onto the six-value enum (`PRIVATE→MEMBER_PRIVATE`, `MEMBER_ONLY→MEMBER_PRIVATE` with an org-admin exception path, `ORGANIZATION_VISIBLE→ORGANIZATION_SHARED`, `SHARED_BY_LINK→CROSS_ORGANIZATION_AUTHORIZED`, `PUBLIC→PUBLIC`), not a second real enum.
- **Resume Output Projection depends on unbuilt Career schema — still a real sequencing dependency, not a conflict to "decide away".** `member-org-admin-config.md` §5's `ResumeOutputProjection` assumes versioned Career Atoms/Molecules (master prompt §77) that don't exist yet. This isn't blocked on a decision — it's blocked on §77's data model existing. Build order, not open question.

## Loop status

| # | Loop | Status | Last run | Notes |
|---|------|--------|----------|-------|
| 1 | Foundation | in progress | 2026-07-16 | **Update 2026-07-16:** Channel/Channel Rod definition reconciled against `Salt_Basin_Workbook_001A_Enterprise_Foundation_v4.xlsx` — see "Flagged items — decided" above. `concept-channel`/`concept-channel-rod` in the registry now state the definition/instance relationship explicitly, backed by the real `journey_rod_types`/`journey_data_rods` split. Workbook also confirmed net-new territory queued for future loops: Fund/PE model and Value Creation Theses map onto the already-seeded-but-inactive `fund_deal`/`portfolio_company`/`value_creation` rod types in `journey_rod_types` (Loop 15 territory — activate + populate Atoms/Molecules/Fields from the workbook's `Fund_Portfolio_Hierarchy`/`Value_Creation_Theses`/etc. sheets, do not build a parallel schema); Chart_of_Accounts/Subledger/Posting_Rules data is a seed-data task against the already-real `gl_accounts`/`accounting_policies`/`accounting_topology_definitions` tables (Loop 13); Pipeline/Forecasts/AR/Fund_Economics/Portfolio_Performance/Payout_Projections have no current home in the governing chain — likely Query Channel/Compressed Query Rod projections over Fund Deal/Revenue Lifecycle rods rather than new base tables, needs design before building (not yet started); Atom_Physical_Properties/Molecule_Physical_Properties is Loop 9's GAP-004 (visual registry); Semantic_Positioning/World_Coordinates as governed data is Loop 9/10; Negative_Convergence_Rules/Atom_Negative_Definitions extends `journey_atom_affinity_rules`' repulsion rules (Loop 11 territory); Approval_Definitions/Approval_Flows/Output_Flows/Output_Contents have no current home anywhere in the chain — genuinely new, not yet scoped to a loop. **Prior 2026-07-12 entry:** Terminology decision, governing chain, and legacy-alias registry already real (`docs/salt-basin-foundation-source-of-truth.registry.json`, `migrations/terminology-migration-registry.json`). **Update 2026-07-12 (Betsy: "anything live was a draft version... run all builds and reconciliations... save existing data in old structure, implement new structure, populate new objects with data through the master build"):** took a full DB snapshot (`migrations/pre-eidos-snapshot-2026-07-12/`, 115 tables, 1,937 rows) before any further change. First live-data migration executed under the terminology registry's pre-approved rule #5 ("Maturity Score" → "Legacy Maturity Observation"): added `historical_observations` (`server/db.js`) and backfilled one row per all 33 live `journey_data_rods` from their `stage_score`, verified live post-migration (33/33). `journey_data_rods.stage_score` itself untouched — additive preservation, not a rename. Still open: renaming `journeyEngine`/`journeyRods.js` at the file/route level (additive alias, not a destructive rename) — not yet done. |
| 2 | Enterprise Hierarchy | reconciled | 2026-07-12 | Pre-build Phase 1 now done (Member Organization Admin identity landed in Foundation doc + registry). Validated §2's rules against Foundation §2: legal-entity tree, brand/product/methodology separation, and the "no false filed-entity status" rule are all already correct (MESA flagged as the only confirmed-filed entity; Salt Basin Highways explicitly documented as platform/methodology, not an entity). §2's metadata field list (`entityStatus`/`filedEntity`/`plannedEntity`/`brandLayer`/etc.) has **no current consumer** — no component renders the entity hierarchy spatially yet (checked: no hardcoded hierarchy exists in `SaltBasinHome.jsx`/`NetWorksPanel.jsx`/elsewhere to refactor either). Deliberately did not create that registry now — doing so ahead of Loop 9/16 (Visual Language/Website) actually rendering it would be the same "schema with nothing underneath" anti-pattern already flagged for the Resume Output Projection. Sequencing dependency, not a gap: build the entity-hierarchy config registry when Loop 9/16 first needs to render it. |
| 3 | Semantic Architecture | in progress (schema real, population thin) | 2026-07-12 | **GAP-001 was stale — corrected.** Verified directly against the live database (115 tables via `information_schema`, not file-reading): the EIDOS L1-L8 schema (`docs/eidos-operating-model-playbook.md`) already persists Ports (`data_ports`/`port_source_objects`/`port_source_fields`), Evidence Atoms with a temporal envelope (`journey_rod_evidence.effective_from`/`effective_to`/`lineage_parent_id`), Semantic Fields (`journey_atom_affinity_rules`), Settlement (`journey_rod_settlement_states`), and Accounting/Reciprocal Inference (see Loop 13). This session also seeded real canonical Atom + Semantic Field definitions straight from master prompt §34's own worked example (8 atoms: ARR/Opportunity Amount/Probability/Contract Effective Date/Onboarding State/Provisioned User Count/Active User Count/Adoption Rate; 2 clusters: Revenue Recognition Readiness, Adoption Signal) into `journey_metadata_molecules`/`journey_metadata_clusters`, which were real-but-empty tables before this pass — verified live (8 atoms, 2 clusters). Real remaining gap, narrower than before: almost every EIDOS table is schema-real but data-empty for the 33 live rods (no per-rod evidence, settlement, or affinity rows populated yet) — population depth is what's left, not persistence. See `context-reconciliation/uncovered-context-register.json` GAP-001 (updated same pass). |
| 4 | Channel Formation | reconciled (prototype layer); flagged (live backend) | 2026-07-12 | Validated §23/§24/§25 creation sequences against `src/lib/journeyEngine/genesis.js` (the spatial-world prototype engine): the three `LEAD_GENESIS_RULES` (internal-sourced, external-intake-org-buyer, external-intake-direct-consumer) already topologically match §23/24/25's canonical rod-formation order exactly. One real gap found and fixed: §25 requires the DTC Member rod's Customer semantic meaning stay queryable via "role convergence" without a duplicate Customer record — nothing implemented this. Added `convergedRoles: ['customer']` to the DTC genesis plan (`genesis.js`) and a `convergedRodTypes` output on `rodHash.js`'s `triangulateEntity()` so a "find Customer rods" query now recognizes the Member rod. Additive only, verified with a direct module test (not a full browser drive — regression-gate not yet run for this loop). **Flagged, not fixed**: the live persisted backend (`server/db.js` bootstrap backfill + `server/lib/journeyRods.js`) has no genesis-rule logic at all — bootstrap unconditionally backfills `member` + `customer` + `revenue_lifecycle` rods for every member regardless of lead source, which contradicts §25's "do not force duplicate Customer records" rule for DTC members. Did not touch this — it's live bootstrap SQL touching real member rows, a Betsy decision (is this backfill intentional simplification, or should real genesis-rule gating apply going forward for new members?), not something to silently rewrite. |
| 5 | Scenarios | reconciled | 2026-07-12 | Located the source of the "2,400+ scenario" reference (`docs/salt-basin-hos-journey-methodology.md:75`): it's a **target scale for a generator that was never built**, not a lost data asset — confirmed no scenario library exists anywhere in this repo/git history (the `journey_scenarios` table in `server/db.js` is real schema, zero seed rows; `server/data/seed.js` seeds nothing into it). Built the missing generator: `server/lib/scenarioGenerator.js` (`generateScenarioMatrix` — pure cross-product of dimension definitions, `estimateMatrixSize`) plus two admin routes in `journeyRods.js` (`GET /scenarios/generate/preview`, `POST /scenarios/generate`) that write into the existing `journey_scenarios` table via `ON CONFLICT DO NOTHING` (additive, matches CLAUDE.md's shared-config-row invariant). Syntax- and unit-verified locally (module import + route file `node --check`); **not executed against the live database** — no DB connection in this session, and it's an admin-triggered action Betsy should run deliberately. `DRAFT_DIMENSION_DEFINITIONS` in that module is explicitly flagged placeholder value data (segment/pricing/industry enumerations I estimated from the HOS doc's illustrative examples, not Betsy's real product catalog) — computes to 21,870 combinations, over the ~2,400+ floor the doc names but built from invented values, not approved ones. **Needs Betsy's real dimension value catalog** before `POST /scenarios/generate` should actually be run — the generator and registry are real now, the specific 2,400+ business scenarios are not, and calling the endpoint with the DRAFT set would write invented scenarios into real config. |
| 6 | Agents | in progress | 2026-07-12 | **Codex continuation:** the live member agent now resolves through a reusable BestyStaff-derived template registry (`server/lib/memberStaffTemplates.js`) instead of one hardcoded identity. Personal Brand Staff and Profile Builder Staff declare purpose, workforce type, permitted tools, writable config roots, external-source authority, and instructions; `memberAgent.js` enforces those boundaries and keeps external access read-only. These are Member Staff, not Channel Rod Staff without explicit assignment. Resume and Financial Staff remain sequenced behind their real pre-build surfaces; 3D agent anchors and multi-user presence remain open. Regression gate pending because browser interaction was not run this session. |
| 7 | Security | in progress (pending regression gate) | 2026-07-12 | Member-vs-Org financial boundary is now structural: Member-owned consent/connection/account tables are locked to `MEMBER_PRIVATE`; raw records have no Organization scope; only allowlisted derived outputs can cross through an explicit, revocable consent action. Policy/provider/account-class behavior resolves through `server/lib/financialPolicyRegistry.js`, not route role conditionals. Unit policy tests pass and production build completed. Full gate cannot pass yet: no Member financial UI exists to drive in-browser, real Plaid source-side query/token-vault integration is not built, and concurrent-user/agent mediation of share actions is not implemented. GAP-005 remains open for governed Port execution. |
| 8 | Context | partially reconciled | 2026-07-12 | §35 (Context Cache): real gap found and fixed — `bestyStaff.js` (the only production agent in the codebase) rebuilt its lead-lookup context from Postgres on every single chat turn, the exact anti-pattern §35 names. Built `server/lib/contextCache.js` (full 13-field record shape: cacheId/agentId/contextDomain/sourceIds/refreshedAt/freshnessThreshold/nextRefreshAt/sourceStateHash/securitySlice/invalidationRules/currentChannelCoordinate/likelyQuestionSet/contextCompleteness/contextConfidence) and wired it into `bestyStaff.js`'s lead lookup (20s freshness window, explicit invalidation on `submit_portfolio_request`). Unit-tested the cache module directly (hit/miss/invalidate all correct) and verified live against the running dev server: `POST /api/agent/bestystaff` still returns 200 with a correct reply after the change, no regression. §34 (multi-tier Agent Context Hierarchy — Context Orchestrator → Domain Agents → Source Agents) **not built**: BestyStaff is a single flat agent; building an empty multi-tier hierarchy config now, with no real Domain/Source agents to populate it, would be scaffolding with nothing underneath (same anti-pattern already flagged for other loops) — sequencing dependency on real Channel Rod Staff agents existing, which is a separate, larger Loop 6 effort. §36 (Proximity-Based Context Refresh) **blocked**: requires a real navigable 3D world with a distinct moving user-object and live distance calculation — Loop 10 (World Navigation) is not started, so there is nothing to compute proximity against yet. |
| 9 | Visual Language | blocked-production | 2026-07-12 | GAP-004 in the coverage register: renderer not fully resolved through the Visual Semantic Registry yet, bespoke material/geometry decisions remain in the existing Three.js component. Dependency update: `salt-basin-visual-metrics` is now **fully done, all 7 phases** (Phase 5's Visual Encoding Registry — `metricVisualEncodingRegistry.js`, one global metric→channel registry with collision validation — plus Phase 7's regression gate and Foundation reconciliation into `docs/salt-basin-foundation-source-of-truth.md` §11's new "Query convergence metric vocabulary" subsection). One positive finding from that skill's Phase 1 audit: `journeyEngine/maturity.js`'s `computeAtomVisual()` already correctly separates semantic math (maturity) from rendering math (scaleY/facet/opacity/etc.) and is the right template to extend, not rebuild. Second dependency: `salt-basin-world-variants` Phase 2 (World Variant Domain, Registry & Semantic Invariant Reconciliation) is now **done** (`src/config/visual/worldVariantRegistry.js` — 6 variants registered, all 11 §III semantic invariants validated 1:1 against `salt-basin-visual-metrics`' Metric Definition Registry with zero gaps) — see `docs/salt-basin-world-variants-progress.md`. `WORLD_REGISTRY` naming-collision decision (content-domain switcher vs. this spec's `WorldVariant`) remains open, not resolved — the new registry was deliberately built as a separate file with no import between the two, so either resolution stays possible. Loop itself remains `blocked-production` — GAP-004's broader claim (every 3D object's material/geometry, not just metric-driven channels, resolving through a registry) is larger than what either dependency phase covers. |
| 10 | World Navigation | not started | 2026-07-12 | Confirmed genuinely not started, not just unrecorded: no camera/travel/focus controller module exists in `src/lib/journeyEngine/*`; whatever camera behavior exists lives ad hoc inside the Three.js scene components themselves (same bespoke-decisions issue GAP-004 already flags for Loop 9). Real build, not a status-tracking gap — needs its own dedicated pass. New dependency: `salt-basin-world-variants` Phase 4/7 (Camera Profile, Interaction Intent layer) — not started; that skill's Phase 1 audit confirmed the current camera (hand-rolled spherical orbit, no OrbitControls) and interaction handling (`SpatialJourneyWorld.jsx`) are real but call scene-mutation functions (`runCustomerOrbit`/`runStageQuery`) directly from selection handlers with no Interaction Intent dispatch layer — the exact anti-pattern that skill's source spec's §XV names. |
| 11 | Query Convergence | meaning redesigned (prototype layer); GAP-001 persistence still open | 2026-07-12 | Re-classified again: `salt-basin-visual-metrics` is now fully done (all 7 phases). The meaning gap this loop was blocked on is closed in the prototype layer — `SpatialJourneyWorld.jsx`'s Customer Orbit no longer displays `"{pctLabel(r.maturity)} converged"` (maturity mislabeled as convergence); it now shows real per-atom Query Relevance with a component breakdown and generated business-meaning sentence on drill-through, plus separately-computed Query Coverage/Confidence/Convergence Stability and a Rod Coherence view replacing raw divergence telemetry. Browser-verified live against the seeded "Ridgeline Data — Buyout by Meridian Capital" entity, zero console errors (see `docs/salt-basin-visual-metrics-progress.md`'s Phase 6/7 changelog entries for the exact verification). Phase 7's regression gate ran clean and the Foundation was reconciled (§11's new "Query convergence metric vocabulary" subsection). "Orbit" is confirmed Foundation-canonical, closing the "Orbin" question there too. Remaining GAP-001 persistence gap unchanged and is the reason this loop isn't `reconciled`/`done`: this is still the prototype (client-side, synthetic) layer only; no equivalent exists on the persisted `journey_data_rods` backend, and "Semantic Hash" as a persisted, content-addressed retrieval key (vs. this module's in-memory hash-on-demand) is GAP-001 territory — a real Loop 11 build task still ahead, distinct from the meaning-redesign work now closed. |
| 12 | Temporal Lineage | substantially implemented (unrecorded) | 2026-07-12 | Also marked "not started" but wrong — `src/lib/journeyEngine/lineage.js` (`getAtomLineage`, `lineageValueAtOffset`, `bumpVersion`) already implements historical (10-week history), current (present), and projected (`pendingBranches`) state per atom, exactly matching §42's historical/current/temporary/projected requirement. Re-classified. Caveat: history/projections are deterministically *synthesized* from the atom's id (seeded random), not derived from real event-sourced proposal/validation/staging/commit/rejection/supersession events — the interface shape is real and correct, the underlying event log is not (same GAP-001 dependency: real persistence of event-sourced lineage isn't built). |
| 13 | Accounting | in progress (schema + basic computation real) | 2026-07-12 | **GAP-003 was stale — corrected.** Verified live: `accounting_policies`/`gl_accounts`/`accounting_topology_definitions`/`journal_entries`/`journal_entry_lines` are real tables with CRUD (`server/routes/eidos.js`), and `server/lib/eidos.js`'s `computeDivergenceBetweenRods()` is a real, callable, persisting divergence computation (axial + density comparison between two rods' `stage_score` and settlement, writes to `divergence_states` — 1 row already existed pre-session from prior testing). Still accurate: this is explicitly "the simplest honest version" per that file's own comment, not the full state-vector divergence math or accounting-topology inference ("what economic world must exist to explain this journal entry") the DSM claim-tree doc describes — no journal entries exist yet for any live rod, so there is nothing yet to infer against. A canonical accounting projection *service* now exists at the CRUD/basic-computation layer; the inference algorithm remains research-stage. |
| 14 | Collaboration | in progress (presence slice real; Agent reasoning not built) | 2026-07-12 | Full §38 scope (real-time transport + Agent collaborative-reasoning compilation across simultaneous users) is too large for one pass — built the real first slice instead of a written plan: `checkpoint_presence` table (`server/db.js`, additive), `server/lib/collaborationRegistry.js` (config: `PRESENCE_ACTION_STATES`, `PRESENCE_TTL_MS`=45s, `PRESENCE_VISIBILITY_RULES` reusing the already-decided six-value Data Scope enum rather than inventing a second visibility taxonomy), and `server/routes/presence.js` (`PUT/GET/DELETE /api/presence/:rodId` — heartbeat, list-who's-here filtered by visibility, explicit departure), wired into `server/index.js`. Poll/heartbeat model, not websocket push — upgradeable later without a schema change, and the master prompt doesn't mandate a transport. Verified live: table created, syntax-checked, end-to-end tested directly against the live DB (real rod + real user, correct visibility gating: an org member sees an `ORGANIZATION_SHARED` presence row, a viewer with no org role does not), test data cleaned up after. **Not built**: the Agent's collaborative-reasoning compilation (§38's worked example — reconciling User A's "annual billing per order form" against User B's "quarterly per proposal") depends on the multi-tier Agent hierarchy from Loop 8 (§34), which itself doesn't exist yet — sequencing dependency, not skipped by oversight. `CROSS_ORGANIZATION_AUTHORIZED` visibility rule is a placeholder approximation (`isRodOwner \|\| orgRole==='admin'`) since no real cross-org-sharing mechanism exists to check against yet — flagged, not overclaimed. Regression gate not run (no browser UI consumes this yet — it's API-only). |
| 15 | Simulation | blocked, partially unblocked at the settlement layer | 2026-07-12 | **GAP-002 partially stale — corrected.** `server/lib/eidos.js`'s `computeRodSettlement()` is real: corroboration-aware settlement density per (rod, molecule) pair, persisted to `journey_rod_settlement_states`, classified into the real surface/water/suspended/sediment/bedrock bands — not a placeholder. Still accurate and still the real blocker for this loop's actual scope (product deals, PE deals, formulas, personal finance, billing, revenue, risk Tributaries): settlement is not wired into `evaluateJourneyRod()`'s gate-qualification math (computing it is still a manual/explicit action), and none of Loop 15's named simulation domains (PE deals, personal finance, billing, risk Tributaries) have any schema or computation yet. The versioned rule engine this loop needs is now closer (settlement math exists) but still absent at the gate-integration and simulation-domain level. |
| 16 | Website | not started (external) | 2026-07-12 | Personal Brand Website/World is Phase 2 of `docs/salt-basin-pre-build-progress.md`, itself dependent on Phase 1 (not started) — corrected here. |
| 17 | Resume & Infographics | in progress (Phases A/0/1 done; Molecule-driven rendering + 3D Career Orbit not yet built) | 2026-07-27 | **Update 2026-07-27 (Phase 1):** Excel Semantic Template + Real AI Extraction + Preview-Before-Persist is done and verified live (see 2026-07-27 Phase 1 changelog entry) — `careerBondingEngine.js` (server-side port of `bonding.js`'s tag-overlap matching, with a real camelCase-tokenizer bug found and fixed during live verification), `careerSemanticTemplate.js`/`careerSemanticImport.js` (downloadable workbook + round-trip parser, one sheet per entry type rather than the plan's literal single sheet — documented deviation), `careerResumeExtraction.js` (the real fix for "Queue N Analysis Passes," which previously only created a permanently-`queued` `career_intake_runs` row), and `POST /api/career/mappings/commit` (the only route that persists, reusing the existing legacy-table + `syncSingleEntry()` write path, not a parallel one). **Still open, next in the approved plan** (`C:\Users\mbets\.claude\plans\noble-snacking-horizon.md`): Phase 2 (Molecule/Cluster-driven resume rendering, replacing the current website-section-driven `Output.jsx` path), Phase 3 (3D Career Orbit Projection — per-job/case-study agent-guided configuration, portfolio maturity scoring, Resume Projection relationship view). **Prior 2026-07-27 (Phase A/0):** Tributary Registry (`server/lib/tributaryRegistry.js` — one config-driven registry/creation path covering both plain `journey_data_rods` rows and satellite tables like `resume_output_projections`, retrofitted onto the existing member→entitlement→public_site/career_master provisioning) and Consent System + Choose Your Path gate (append-only `consent_actions` table, `consentRegistry.js`, `CareerConsentGate.jsx`, `ChooseYourPathScreen.jsx`/`UploadDataScreen.jsx`/`CareerMasterEntryPoint.jsx`) — both done and verified live end-to-end. Terminology corrections applied throughout: bonding rules (not magnetic field rules), Channel Journey, Currents, Tributary's precise two-case definition. **Prior 2026-07-16 finding, still relevant:** Career Atom migration executed for real — all 7 legacy `career_*` tables have a real Career Atom/Molecule vocabulary (`server/lib/careerAtomRegistry.js`, 79 atoms/7 molecule types) seeded into `journey_metadata_molecules`/`journey_metadata_clusters`, and all 144 of user 1's real legacy rows (1,137 atom-instances) migrated into `journey_rod_evidence` attached to a real `career_master` Channel Rod — verified live, idempotent, legacy tables untouched. `computeRodSettlement()` treats multiple legitimate job entries sharing one atom key as *conflicting* evidence (caps density at "suspended") rather than recognizing "many valid Molecule instances of the same Atom type" — not fixed, flagged for a future settlement-model refinement. The basic Resume Output Projection layer (fingerprint/staleness/lineage) was built 2026-07-16 (separate entry below) — what's still missing is the *rendering* redesign (Phase 2) and the richer configuration experiences (Phase 1/3) on top of it. |
| 18 | Performance | not started | 2026-07-12 | Confirmed genuinely not started: no LOD/instancing (`InstancedMesh`) anywhere in `src/`. Sequencing dependency, not just unbuilt — meaningful 50,000-node performance work needs Loop 10's real navigation controller and Loop 9's real renderer to exist first; testing performance against the current bespoke Three.js scenes wouldn't validate the eventual production renderer. |
| 19 | Existing Data | in progress | 2026-07-12 | **Item (1) executed this pass**, per Betsy's explicit follow-up authorization ("anything live was a draft version... save existing data in old structure, implement new structure, populate new objects with data through the master build"). Took a full snapshot first (`migrations/pre-eidos-snapshot-2026-07-12/`, 115 tables, 1,937 rows, before any change). Then: added `historical_observations` (`server/db.js`) per `terminology-migration-registry.json`'s own pre-approved rule #5 ("Maturity Score" → "Legacy Maturity Observation," `automaticMigrationAllowed: true`) and backfilled one row per all 33 live `journey_data_rods` from `stage_score` — verified live, 33/33, `journey_data_rods.stage_score` itself untouched (additive preservation, not a rename, per that rule's own constraint: "do not equate it to journey coordinate or settlement"). Also seeded real Atom/Semantic-Field vocabulary into the previously-empty `journey_metadata_molecules`/`journey_metadata_clusters` from master prompt §34's worked example (see Loop 3). **Item (2)** (renderer maturity → projection-relative settlement) remains blocked on GAP-002's full versioned rule engine (see Loop 15) — `computeRodSettlement()` exists now but isn't wired into gate math yet, so this item is unblocked further but not complete. |
| 20 | Project Context | reconciled | 2026-07-12 | Re-read the untracked docs not yet cross-linked from this tracker: `eidos-business-rules-mapping.md`, `eidos-operating-model-playbook.md`, `foundation-channel-update-comparison-2026-07-12.md`, `salt-basin-architecture-implementation-continuation.md`, `salt-basin-divergent-state-mechanics-claim-tree.md`. All are internally consistent with what's already recorded here (EIDOS is schema/API/admin-UI only, matches GAP-001–003's "blocking-production" status; DSM claim-tree doc is IP/patent-track material, not a build gap). One real, not-yet-cross-linked finding worth surfacing here so it isn't lost: `eidos-business-rules-mapping.md` itself flags **no home anywhere in the schema for plain-language business rules + test cases** (`journey_gate_definitions.dependency_rules` is the closest thing — structured predicates only, no human-readable rule text or test-case field) as "the largest real gap" in the ontology mapping, and flags that the Member Portal Crystal spec's Agent Hierarchy language and the DSM data-model's `AGENT_BOUNDARY` entity are drifting apart across documents. Neither requires a live-DB change to fix (both are schema-additive), but both were sitting undiscoverable from this tracker — added here so a future `/salt-basin-build` run doesn't miss them. GAP-006 (no test runner, accounting/resuspension scenario tests absent) remains open — unchanged this pass. |

Status values: `not started` / `in progress` / `blocked` / `reconciled` (loop's findings have been applied structurally, not just listed).

## Changelog

- **2026-07-12** — `salt-basin-visual-metrics` reached Phase 7 and is now fully done (all 7 phases). Updated Loop 9 and Loop 11's rows to reflect it: Loop 9 gains the completed Phase 5 Visual Encoding Registry dependency but stays `blocked-production` (GAP-004's scope is broader than metric-driven channels alone); Loop 11 is re-classified from "mechanism implemented, meaning not yet redesigned" to "meaning redesigned (prototype layer); GAP-001 persistence still open" — the convergence/maturity conflation this loop was blocked on on 2026-07-12 earlier today is closed and browser-verified, but the persisted-backend gap (GAP-001) is a separate, still-open build task. Did not run a full `/regression-gate` pass for either loop as a whole (Channel creation rules, full deal test, concurrency, Agent authority) — only `salt-basin-visual-metrics`' own Phase 7 gate, scoped to its diff, has run. Neither loop is marked `reconciled`/`done` on this tracker's own terms yet.

- **2026-07-12** — Ran `/salt-basin-build` with no argument. Picked Loop 14 (Collaboration) deliberately over Loops 9/10/11 — those three have two sibling skills (`salt-basin-visual-metrics`, `salt-basin-world-variants`) actively mid-flight on their exact dependencies per this same tracker's recent entries, and piling a third concurrent editor onto those files right now is pure collision risk with no scope benefit. Loop 14 was genuinely untouched and next in table order. Built the real first slice of §38 rather than a written plan: `checkpoint_presence` table (additive), `server/lib/collaborationRegistry.js` (presence action states, TTL, visibility rules reusing the already-decided six-value Data Scope enum), `server/routes/presence.js` (heartbeat/list/departure, wired into `server/index.js`). Verified end-to-end directly against the live database with a real rod/user (correct visibility gating, cleaned up after). Did not build the Agent collaborative-reasoning half of §38 — genuinely sequenced behind Loop 8's still-unbuilt multi-tier Agent hierarchy, not skipped. Updated Loop 14's row from a stale "not started" (predates today's other loop work) to reflect this.

- **2026-07-12** — Loop 9/11 dependency continuation advanced Visual Metrics Phase 6: removed the `maturity % converged` UI bug, removed raw divergence telemetry from the primary panel, rendered registry-defined query metrics and Coherence, and made convergence radius a configured Query Relevance encoding. Phase 6 remains in progress for drill-through/business narrative and browser gate.

- **2026-07-12** — Loop 9 dependency continuation completed Visual Metrics Phase 5. The Visual Encoding Registry is real and validated; a post-audit removed the existing atom renderer's multi-meaning maturity encoding so permanent color, opacity, geometry/material, risk, and maturity no longer compete for the same visual properties. Phase 6 can now safely replace the legacy query-result UI.

- **2026-07-12** — Loop 9 dependency continuation completed the Visual Metrics Phase 3–4 engine prerequisites. Query convergence and all seven Data Rod metrics now have distinct, configured formulas and explicit breakdowns. Existing evidence-settlement density and correlated-envelope parameters were brought under methodology configuration. Visual Encoding Registry work is now unblocked; no UI meaning was changed ahead of it.

- **2026-07-12** — Loop 9 dependency continuation: advanced Visual Metrics Phase 3 instead of inventing a visual encoding ahead of semantic mathematics. Added configurable Query Relevance/Coverage/Confidence/Stability methodology and engines; wired real query-context Relevance/Coverage into rod-hash projections. No rendering changes yet. This preserves the required sequence: semantic meaning first, Visual Encoding Registry only after Phases 3–4 are real.

- **2026-07-12** — Re-ran Loop 8 (Context) under the strengthened no-hardcoding interpretation. Pre-audit found BestyStaff's cache policy embedded in `bestyStaff.js` (agent/domain identity, 20-second freshness, invalidation description, source IDs, and cache-key formats) plus a silent 60-second fallback in `contextCache.js`. Added `server/lib/agentContextRegistry.js`; BestyStaff's published/default intake config now selects `contextPolicyKey`, and the route resolves all cache behavior from that versioned registry. `contextCache.js` now rejects missing freshness policy instead of inventing a default. Tests prove registry resolution/key rendering and rejection of implicit freshness configuration. Post-audit scan: no cache timing/source/invalidation semantic values remain in the consuming route. Loop 8 remains partial overall because multi-tier Context Orchestrator/Domain/Source agents and proximity refresh still depend on later agent/world-navigation work.

- **2026-07-12** — Loop 7 continuation/config-audit correction: the first Member financial UI pass incorrectly placed the new tab, provider enumeration, provider defaults, and account-class defaults directly in component/route code. Betsy immediately rejected that as contrary to the master-build rule. Corrected before handoff: Member tabs now resolve from `defaultMemberConfig.navigation.memberTabs`; the route enumerates `FINANCIAL_PROVIDERS`; provider labels/order/availability, privacy and credential notices, default provider/account classes, liability mappings, scopes, and permitted share-output definitions resolve from `financialPolicyRegistry.js`. `MemberFinancialPanel.jsx` consumes those definitions and contains rendering capability only. Hardcode scan of the consuming UI/route is clean for the financial semantic values. This correction reinforces that every subsequent loop must convert product assumptions during the pre-audit, not after Betsy catches them.

- **2026-07-12** — Ran Loop 7 (Security) through its first structural slice by invoking Pre-build Phase 4. Pre-audit found no central security/retention/financial-provider registry; added `server/lib/financialPolicyRegistry.js` and routed connection/account/share rules through it. Added Member-private persistence and `/api/member-financial` endpoints; Organization authority never implies personal-finance access, and organization readers receive only explicit derived projections (not source account IDs). Post-audit: converted provider classes, account/liability mappings, security/retention IDs, Data Scope, and share-output classes to configuration. TEMPORARY PROTOTYPE DEBT: provider list exposure is route-local; full policy evaluation is not yet shared across all agents/routes; Plaid is configuration-only; no financial UI or Channel-Rod/Atom population exists. Regression gate: **PASS** syntax, policy unit tests (4/4), production bundle, Foundation alignment, and architecture scan for this diff; **NOT RUN / therefore overall FAIL** interacting-user browser flow, real provider refresh, agent authority/concurrency, and cross-Rod/temporal behavior because those surfaces do not yet exist. Loop remains `in progress (pending regression gate)`.

<!-- Newest entry on top. One entry per /salt-basin-build invocation. -->

- **2026-07-27** — Career Configuration Overhaul, Phase 1 (Excel Semantic Template + Real AI Extraction + Preview-Before-Persist), continuing directly from Phase A/Phase 0 in the same session. Added `xlsx`, `mammoth`, `pdf-parse` deps.
  - **`server/lib/careerBondingEngine.js`** (new): server-side port of the free-text matching half of `src/lib/journeyEngine/bonding.js`, reusing its exact `tagOverlapRatio` math rather than re-deriving a second affinity calculation. Every Career Atom's tag set is derived from its label/entryType/sourceColumn tokens; `matchTextToCareerAtom()` bonds one free-text label (an Excel header or an AI field name) to the closest governed atom, with an exact-match short-circuit (affinity 1.0) plus a fuzzy tag-overlap fallback. **Real bug found and fixed during live verification**: the initial tokenizer didn't split camelCase, so AI-proposed field keys like `startDate`/`keyMetrics`/`yearsExp`/`resumeLanguage`/`nameUsed`/`firstEarned` arrived as one fused token and never overlapped with their atom's multi-word label — verified via a real resume upload (7 unresolved headers out of ~30 fields). Fixed by splitting camelCase before lowercasing; re-verified the same upload afterward: 0 unresolved headers.
  - **Career Bonding Rules seed** (`server/db.js`, additive): `journey_atom_affinity_rules` was real schema, zero rows for any Career cluster/atom pair — seeded all 79 atoms' deterministic entryType membership (`minimum_affinity=1.0`) so the semantic template's reference sheet and the bonding engine's floor both have real data to read, not an empty table.
  - **`server/lib/careerSemanticTemplate.js`** (new, `GET /api/career/semantic-template`): generates a real downloadable Excel workbook — README, Import_Manifest, Career_Atom_Definitions (all 79 atoms), Career_Molecule_Definitions (7 entry types), Career_Bonding_Rules (the seeded rows above), and one "Entries - ..." fill-in sheet per entry type with real example rows. Design choice, not the plan's literal single "Career_Entries" sheet: one sheet per entry type with exact-label headers, so a well-behaved re-upload needs no fuzzy matching at all — far more usable in Excel than one 82-column sheet, and documented as a deliberate deviation, not an oversight.
  - **`server/lib/careerSemanticImport.js`** (new, `POST /api/career/semantic-import`): parses an uploaded workbook's "Entries - ..." sheets, resolves each header to a Career Atom (exact label match first, `careerBondingEngine.js` fuzzy fallback for renamed headers), and returns a proposal — does not persist.
  - **`server/lib/careerResumeExtraction.js`** (new, `POST /api/career/resume-analysis`): the actual missing piece behind "Queue N Analysis Passes," which previously only created a `career_intake_runs` row that stayed `queued` forever (see the 2026-07-16 Loop 17 entry below). Extracts text via `mammoth` (.docx) or `pdf-parse` v2's `PDFParse` class (.pdf, `new PDFParse({data: buffer}).getText()` — not the v1 functional API), then calls Claude with a forced tool call (`propose_career_mappings`, modeled on `bestyStaff.js`'s tool schema style but a single one-shot extraction, not a multi-turn conversation) to propose structured facts grouped by entry type. `bondProposedMappings()` re-keys the AI's own field names to resolved atomKeys via the bonding engine, producing the identical proposal shape `careerSemanticImport.js` produces — one shape, one preview UI, one commit route, regardless of source.
  - **`POST /api/career/mappings/commit`** (`careerMaster.js`, the only route in this group that writes to the database): inserts each confirmed entry into its real legacy `career_*` table (the actual editing surface per this file's own header comment, and per CLAUDE.md) via `atomDefinitionByKey()` (new reverse-lookup helper in `careerAtomRegistry.js`), then reuses the already-shipped `syncSingleEntry()` to populate `journey_rod_evidence` — the same single write-path every other Career Master create already uses, not a second parallel evidence-only write. Also records a `career_intake_runs` row (`status: 'completed'`) documenting the commit — done at commit time, not pre-emptively at analysis time, since that's when the outcome is actually known.
  - **Scope decision, explicit**: neither `/semantic-import` nor `/resume-analysis` persists the raw uploaded file to Supabase or creates a `career_intake_documents` row — the proposal is transient (held in React state until confirmed), which is what "preview before persisting" requires. The legacy `career_intake_documents`/`career_intake_runs` upload flow (Supabase storage, redaction-ack booleans) remains a separate, still-dormant system for this new path — re-demanding the same acks the new Phase 0 consent gate already collected would be redundant, not a gap silently left unfixed.
  - **`src/components/admin/CareerMappingPreview.jsx`** (new): shared preview/edit UI for both paths — every proposed entry and field has an include/exclude checkbox and an editable value; unresolved headers are shown, not hidden. Only "Confirm & Save" calls the commit route. Atom-reassignment for unresolved headers (letting the member manually pick a different atom) is out of scope for this pass — unresolved fields are shown transparently and skipped, not silently dropped or guessed at.
  - **`src/components/admin/UploadDataScreen.jsx`** (rewritten): the three real actions — download template, upload a filled-in template, upload a resume — now call the real endpoints and route into `CareerMappingPreview` instead of gracefully 404-ing.
  - **Verified fully live**, both API-level and in the running browser, with a fresh test account: downloaded the real template (73KB, all 12 sheets present with correct headers and example rows) and re-uploaded it unmodified — 0 unresolved headers, every field an exact match; committed one job entry through `/mappings/commit` and confirmed it landed correctly in `career_jobs`, synced into `journey_rod_evidence` via the existing `syncSingleEntry`, and recorded a `completed` `career_intake_runs` row; uploaded a real plaintext resume to `/resume-analysis` and confirmed Claude proposed 2 jobs/3 skills/3 tools/2 engagements/1 certification with sensible confidence scores, catching and fixing the camelCase tokenizer bug in the process. Also drove the actual browser UI end-to-end: consent gate → Choose Your Path → Upload Data screen rendering all three real options with no console errors. Test user, rows, and scratch files all cleaned up after — verified zero orphan rows.
  - **Still deferred to Phase 2/3** (per the approved plan's ordering — `C:\Users\mbets\.claude\plans\noble-snacking-horizon.md`): Molecule/Cluster-driven resume rendering (`Output.jsx` still reads website sections, not Career Atom evidence) and the 3D Career Orbit Projection experience.

- **2026-07-27** — Career Configuration Overhaul, Phase A + Phase 0 (approved plan: `C:\Users\mbets\.claude\plans\noble-snacking-horizon.md`). Betsy's request spans four subsystems (consent/gate, Excel semantic import + AI extraction, Molecule/Cluster-driven resume rendering, 3D Career Orbit Projection); this pass built the foundational two. Terminology corrections given directly by Betsy during planning and applied going forward: "magnetic field rules" → **bonding rules** (matches the existing `bonding.js` naming); a Channel instance is a **Channel Journey**; a Channel Journey has **Currents** (stages with entry/exit rules carrying Atoms/Molecules through it); a **Tributary** precisely means either (a) multiple Channel Journeys sharing Atoms/Molecules, or (b) a sub-Channel-Journey (alternate route) linked to its main-route Channel Journey. This adds "Channel Journey" as a term alongside the already-canonical Channel/Channel Rod/Journey-as-legacy-alias in `docs/salt-basin-foundation-source-of-truth.registry.json` — flagged, not reconciled into that registry yet; follow-up, not blocking.
  - **Phase A — Tributary Registry** (`server/lib/tributaryRegistry.js`, new): per Betsy's explicit plan-mode correction ("needs to be configured flexibly... follow the same architectural principles and be able to expand to the larger architecture"), one config-driven registry — `TRIBUTARY_TYPES` names every valid relationship (`member_entitlement_provisioning`, `member_public_site_provisioning`, `member_career_provisioning`, `career_resume_projection`, `resume_output_layout_tributary` [not yet wired], `member_organization_provisioning` [not implemented, named slot for later]) with `cardinality` and either a `childJourneyType` (plain `journey_data_rods` row) or `childTable` (specialized satellite table, e.g. `resume_output_projections`) — one registry covering both uniformly, not per-feature logic. `validateTributary()` + `createJourneyTributary()` are the single creation path (INSERT into `journey_data_rods` with correct `parent_rod_id`/`rod_relationship_type`, plus a `rod_created` event row).
  - **Retrofit** (`server/lib/memberProvisioning.js`): per Betsy's explicit instruction to retrofit *now*, not defer — replaced the private `insertRod()` calls for the entitlement rod and its content rod (public_site/career_master) with `createJourneyTributary()` calls. Same columns/values, no behavior change, just routed through the shared validated path.
  - **Re-verified live** with a fresh test member: provisioned both modules end-to-end, confirmed correct `parent_rod_id`/`rod_relationship_type` at every level and correct stage progression to `provisioned` — no regression, test data cleaned up.
  - **Phase 0 — Consent System + Choose Your Path Gate**: new `consent_actions` table (`server/db.js`, additive, modeled on the existing `audit_log` pattern) — append-only, timestamped, never overwritten (`user_id`, `consent_type`, `action` granted/revoked, `consent_version`, `context` JSONB, `ip`, `user_agent`). `server/lib/consentRegistry.js` defines the `career_portfolio` consent type (3 acknowledgements, reworded per Betsy: checkbox 1 now reads "I will upload redacted information..." not "I have uploaded...") with a `consentVersion`; `hasCurrentConsent()` only counts a grant against the *current* version, so a future wording change re-prompts everyone automatically. Two routes on `careerMaster.js` (`GET/POST /api/career/consent-status`, `/consent`).
  - **UI**: `CareerConsentGate.jsx` blocks all career configuration until `granted:true`, sourcing checkbox text from the server registry (not hardcoded client-side) so wording changes don't need a client deploy. `ChooseYourPathScreen.jsx` (Enter Career Orbit — disabled, "coming soon" badge, this is Phase 3 territory; Upload Data; a link tile back to the existing site editor for public-site career layout, reusing the already-shipped `content`/`config` member tabs rather than rebuilding). `UploadDataScreen.jsx` (Download Semantic Template / Upload Existing Resume — both call Phase 1 endpoints that don't exist yet and gracefully toast "coming soon" on 404, an honest placeholder seam rather than a silent failure). `CareerMasterEntryPoint.jsx` wraps the gate + local view-state (`choose`/`upload`/`orbit`/`classic`) and is now what `AdminShell.jsx`'s member-scope `careerMaster` tab renders (`MyResumePanel` is now also wrapped in the same gate). Found and removed one piece of dead code in `AdminShell.jsx` in passing: an unreachable duplicate `careerMaster` branch shadowed by an earlier, more generic line.
  - **Verified fully live** in the actual browser, two real accounts: `demo-member@example.com` (pre-existing consent) passed straight through to Choose Your Path with no re-prompt; a fresh `consent-gate-test@example.com` was correctly blocked, showed the exact reworded checkbox text, only enabled "I Agree — Continue" once all three were checked, and on submit recorded a real `consent_actions` row (confirmed via direct query: `action: 'granted'`, correct `consent_version`, real timestamp) and transitioned to Choose Your Path. Test account and row cleaned up after.
  - **Deferred to Phase 1/2/3** (per the approved plan's explicit ordering, not oversight): Excel semantic template + AI resume extraction + preview-before-persist; Molecule/Cluster-driven resume rendering; the 3D Career Orbit Projection experience and its agent-guided per-job/case-study configuration, maturity scoring, and Resume Projection relationship view. `UploadDataScreen.jsx`'s two buttons are already wired to call the Phase 1 endpoints the moment they exist.

- **2026-07-16** — Betsy: personal brand website deferred to another session; "let's do the resume output creator." Built the real `ResumeOutputProjection` layer per master-org-admin-config.md §5 (previously only Career Atoms existed underneath it — the actual projection/lineage/staleness mechanism itself was missing):
  - **Schema** (`resume_output_projections`, additive): tracks `preset_id`/`preset_name`/`included_sections` (the existing `member_json_store` "resume_presets" configuration, untouched — per §5's own rule not to create a second resume data record), `career_state_fingerprint` + `atom_count` (the Career Atom snapshot), `output_status` (draft/approved/published/archived), `lineage_root_id` (self-referencing chain for regenerations).
  - **`server/lib/resumeProjection.js`**: `computeCareerStateFingerprint()` — a deterministic SHA-256 over every Career Atom evidence row on a member's `career_master` rod; `createResumeOutputProjection()` snapshots it; `checkStaleness()` compares current vs. snapshotted fingerprint without ever auto-regenerating (§5: "Do not automatically rewrite an approved resume").
  - **`/api/resume-outputs`** (member-scoped) + `api.js` client methods.
  - **UI** (`MyResumePanel.jsx`, additive — did not touch the existing preset/layout/agent-tailoring logic): a "Save as Output" button next to the existing Preview/Print actions, and a "Resume Output History" list showing generated snapshots with status-transition buttons (Approve → Publish, Archive) and — when stale — the master prompt's own example wording: "Your Career Channel has N atom update(s) not reflected in this resume output."
  - **Verified fully live**: generated a real output from the `demo-member` test account through the actual browser UI (button click → network request → row created → list re-rendered), clicked Approve and confirmed the status transition round-tripped correctly. Verified staleness detection against Betsy's real 1,137-atom Career Master (not test data): a fresh projection reports `isStale: false`; inserting one real evidence row flips it to `isStale: true` with the correct atom-count delta; test row and test projection cleaned up immediately after, no residue in real data.
  - **Scope note**: this is the *projection/lineage* layer (§5) — actual multi-format download generation (§6: PDF/DOCX/ATS-text/structured JSON with preserved lineage) and public-website exposure controls (§7) still route through the pre-existing `/output/resume` live-template renderer, which does not yet read from a specific projection snapshot (it always renders current preset + current Career Master state). Wiring §6/§7 to render *from* a saved projection specifically (so a published output stays exactly what was approved, independent of later edits) is the natural next increment, not attempted this pass.

- **2026-07-16** — Betsy asked how to test the Member Entitlement/Career Atom work, then: "let's build the admin configurable UI since that's what the members are going to use." Built the first real member-facing surface for this pipeline:
  - **`GET /api/member-entitlements`** (`server/routes/memberEntitlements.js`): member-scoped, returns the caller's Member Rod + all Entitlement Rods with module label, current stage, provisioning origin, welcome-email status, linked content rod, and a real usage summary (via `usageTracking.js`) — stage list comes from `provisioningPolicyRegistry.resolveProvisioningTemplate()`, not hardcoded, so an org's own configured template changes what renders without a code change.
  - **`MemberEntitlementsPanel.jsx`** ("My Access" tab): visual stage-progress per module, welcome-email confirmation badge, linked content-rod status, login count.
  - Discovered `CareerMasterPanel.jsx` already defaults to `scope='member'` and is already properly user-scoped end-to-end (`server/routes/careerMaster.js` filters every query by `req.user.id`) — it was simply never wired into the member nav, only the admin one (`careerMaster: () => <CareerMasterPanel scope="admin" />` in `AdminShell.jsx`'s admin-only `TAB_COMPONENTS` registry). Added a `careerMaster` branch to AdminShell's separate member-tab resolution chain (member tabs resolve through their own `if (componentId === ...)` chain, not the admin `TAB_COMPONENTS` registry — a real architectural distinction worth remembering for future member-tab additions) rendering `<CareerMasterPanel scope="member" />`.
  - Added both `entitlements` and `careerMaster` to `defaultMemberConfig.js`'s `memberTabs` (new members only, per the seed/bootstrap-never-touches-existing-member-rows invariant).
  - **Verified fully live, not just built**: created a real, persistent test member (`demo-member@example.com`) via the actual `provisionDefaultModulesForNewMember` path, logged in through the real browser UI (not a script), and confirmed: the login itself correctly triggered `handleFirstLogin` (Member Rod → `member_active`, login recorded), the new "My Access" tab renders both entitlements with correct stage/origin/email-status/linked-rod data pulled live from the API, and the new "Career Master" member tab renders and is correctly isolated per-user (shows empty state for the new member, not Betsy's real data).
  - One real hiccup during verification: the shared dev API server (port 3001) was down at the start of this session (unrelated to any specific edit — likely a restart race from the heavy concurrent-session file activity) and had to be restarted; a plain browser click on the login button also initially failed to fire its network request (traced to the automation click not reliably dispatching React's synthetic event) — worked once the form was submitted via a proper dispatched event. Noted for future browser-automation verification in this app, not a product bug.
  - **Next**: this is a read-only status view. The Personal Brand Website module (existing `content`/`config` member tabs, backed by `member_sites`/`member_configs`) and the Resume Output Creator (`MyResumePanel`, already real) are the actual "give him the personal brand website configuration and resume outputs creator" deliverables from the original request — both already exist and work today; what's still missing is threading entitlement *gating* through them (e.g., don't show the Resume tab's output-generation actions until its Entitlement Rod reaches `active`), not building them from scratch.

- **2026-07-16** — Betsy: "let's continue, let's migrate the career master database to atoms" (the next step queued by the prior entry). Migrated the legacy `career_jobs`/`career_skills`/`career_tools`/`career_engagements`/`career_domains`/`career_certifications`/`career_deals` tables to a real Career Atom/Molecule model per master prompt §77:
  - **Registry** (`server/lib/careerAtomRegistry.js`): table-driven field mapping (7 source tables → 7 Career Molecule "entry types" → 79 generated Career Atom definitions), same generator philosophy as the L2 scenario matrix — not 79 hand-authored definitions. Excludes pure record-management columns (id/order_index/created_at/updated_at/user_id) and presentation choices (icon/accent_color) as not being governed business facts.
  - **Vocabulary seed** (`server/db.js`, additive): all 79 atoms + 7 molecule/cluster definitions inserted into the already-real `journey_metadata_molecules`/`journey_metadata_clusters` tables (previously empty for career content) — verified live.
  - **Data migration** (`server/lib/careerAtomMigration.js`): `migrateCareerDataForUser`/`migrateCareerDataForAllUsers` — ensures a real `career_master` Channel Rod per user, then migrates every legacy row into `journey_rod_evidence` (one atom-instance per non-null column, `source_reference` = `<table>:<id>` groups atoms from the same source row into one Molecule instance). Additive only — legacy tables never modified. Idempotent via the existing `journey_rod_evidence` UNIQUE constraint.
  - **Real bug found and fixed mid-migration**: the `db` adapter (`server/db.js`) returns jsonb source columns (`career_engagements.roles`/`outcomes`/`metrics`/`scenarios`, `career_domains.items`) as raw JSON-text strings rather than parsed values (confirmed as a pre-existing, systemic characteristic of this codebase's `sql.unsafe`-based adapter — `journey_metadata_clusters.molecule_keys`, populated by already-shipped code, has the exact same characteristic). The first migration pass re-stringified those already-JSON-text values before storage, double/triple-encoding 119 evidence rows. Fixed by parsing jsonb-source columns once before re-encoding (`isJsonbSourceColumn()` in the registry); deleted and re-migrated the 119 affected rows; verified a single `JSON.parse()` now yields the correct real array for every case, consistent with how the rest of the codebase already reads jsonb columns.
  - **Verified against the live database**: 144 real rows (user id 1 — the only user with legacy career data) → 1,137 atom-instance evidence rows across a real `career_master` rod (id 44); a spot-checked job entry (`Blackbaud, Inc.` / `Contract Processing Specialist`) round-tripped correctly; re-running the migration is a confirmed no-op (0 new, 1,137 skipped); `server/lib/eidos.js`'s existing `computeRodSettlement()` runs successfully against the new evidence with no code changes needed.
  - **Real nuance surfaced, not fixed**: `computeRodSettlement()` treats several legitimate Career Atom instances sharing one atom key (e.g. 10 different `career_job_title` values, one per job) as *conflicting* evidence, capping density at "suspended" — it was designed for single-governed-value atoms (ARR) and doesn't yet distinguish "many valid Molecule instances" from "unreconciled disagreement." Flagged in Loop 17's row rather than silently miscounting real career depth as low confidence; a real fix would need settlement grouped by `source_reference`, not attempted this pass.
  - **Next**: build the actual Resume Output Projection (member-org-admin-config.md §5) rendering against this now-real Atom substrate — that was the dependency this whole migration existed to unblock.

- **2026-07-16** — Betsy: build the direct-to-consumer Member Portal onboarding experience — lead conversion should provision a Member Channel Rod, which forms a Member Entitlement Channel Rod per module (Personal Brand Website, Resume/Career), each with a linked content Channel Rod (`public_site`, `career_master`), welcome email, password-reuse-from-lead security, usage tracking, and first-login stage transitions — all through the global (but DTC-only-for-now) provisioning process, fully configurable. Built real, working infrastructure, not a plan:
  - **Terminology decision** (Betsy explicitly deferred this call to "whatever the real master-build term is"): a Member Channel Rod spawning a Member Entitlement / Public Site / Career Master rod is a **typed rod relationship** (`parent_rod_id` + `rod_relationship_type`), not a Tributary — Tributary is reserved for bounded scenario divergence within one rod's own lifecycle, per `terminology-migration-registry.json`'s own "Member Organization Branch → Member Organization Relationship" precedent (that migration explicitly rejected calling an analogous rod-to-rod link a Tributary).
  - **Schema** (`server/db.js`, additive): `journey_data_rods` gained `parent_rod_id`, `rod_relationship_type`, `module_key`, `provisioning_origin`, `onboarding_email_status`, `first_login_at`. Three new real Channel Rod types registered: `member_entitlement`, `public_site`, `career_master`. Refined `idx_rods_user_type`'s WHERE clause (DROP+CREATE, no data touched) to exclude `member_entitlement` so one member can hold multiple entitlement rods (one per module), given its own `(user_id, module_key)` uniqueness instead.
  - **Config** (`server/lib/provisioningPolicyRegistry.js`): Salt Basin default stage-gate template (`requested → credentials_prepared → welcome_email_sending → provisioned → onboarding → active`), module→content-rod-type mapping, per-module tracked-interaction lists, and the security policy (password-reuse rule, MFA explicitly recorded as `{status: 'roadmap', implemented: false}` per Betsy's ask — not built, but now a real, discoverable line in code). Org-scoped override seam (`resolveProvisioningTemplate(orgId)`) exists but always returns the Salt Basin default today — no org has asked to diverge yet, built the seam not a speculative override table.
  - **Core logic** (`server/lib/memberProvisioning.js`): `provisionEntitlement()` (idempotent — a second call for the same user+module is a no-op), `provisionDefaultModulesForNewMember()`, `handleFirstLogin()`. Password handling: discovered the *existing* `POST /api/leads/public/:publicId/convert` route already does exactly what Betsy described — copies the lead's `password_hash` verbatim into the new `users` row (no re-hash, same password) — so this module hooks in *after* that, and only issues a **password-set link** (reusing the existing, already-reviewed `password_reset_tokens` + `/reset/<token>` mechanism — never a plaintext password by email) for the case where no lead-conversion history exists. This is real "top of the level security practices": the plaintext password is never known to this system in either branch.
  - **Email** (`server/lib/email.js`): `sendMemberEntitlementWelcome()`, config-gated the same way `sendNewLeadAlert` already is, brand-consistent with existing templates.
  - **Usage tracking** (`server/lib/usageTracking.js`): reuses the existing `analytics_events` table (not a new table) — `recordLogin()` wired into `POST /api/auth/login`, `recordInteraction()` validated against the config's tracked-interaction list, `getEntitlementUsageSummary()` computes login count/interactions on demand.
  - **Wired into real flows**: `server/routes/leads.js`'s conversion route now calls `provisionDefaultModulesForNewMember` right after `ensureMemberJourneyRods`; `server/routes/auth.js`'s login route calls `recordLogin`.
  - **Verified**: `node --check` on every touched/new file; a full end-to-end run against the live database (isolated test user, cleaned up after) confirmed both modules provision correctly, the second provisioning call is a true no-op, `handleFirstLogin` correctly moves the Member rod to `member_active` and both Entitlement rods to `onboarding` while leaving the `public_site`/`career_master` content rods untouched at `requested`, and interaction recording + the usage summary rollup are correct.
  - **Concurrent-session collision, caught before it broke anything**: another session was independently building this same feature in parallel and had already added an isolated `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` backfill block for 5 of these 6 columns near the top of `bootstrap()` — correctly diagnosing that adding columns only inside the `CREATE TABLE IF NOT EXISTS` block (which no-ops on an existing table) would never actually apply them, and would make the later `CREATE UNIQUE INDEX` statements referencing `module_key`/`parent_rod_id` throw and abort `bootstrap()` for every process importing `db.js`. Added the one column their list was missing (`first_login_at`) to that same block rather than duplicating it. Confirmed via `information_schema` that the full column set and all three rod types are live, and confirmed via a standalone `bootstrap()` run that the file loads cleanly end-to-end.
  - **Deferred, per Betsy's own explicit scoping** — not oversights: the organization-provisioning path (org rod → multiple Member rods, email-identifier duplicate-detection, account-merge tooling), real MFA, and Career Atom migration (see Loop 17's row — `career_master` is a real Channel Rod now, but the underlying `career_*` tables are not yet Atom-modeled, so there's no Career content behind it yet). Next: migrate Career Master to Career Atoms so the `career_master` rod has real content, then build the Resume Output Projection on top of it.

- **2026-07-16** — Betsy shared a new source workbook (`Salt_Basin_Workbook_001A_Enterprise_Foundation_v4.xlsx`, 55 sheets, "Enterprise Foundation") and asked for a full gap comparison against the built codebase, then said "add it all" with two explicit decisions: (1) Channel/Channel Rod relationship = "Channel will define the definition, Channel Rod reflects the persistent instance of the Channel" — rejecting the workbook's own literal claim that Channel Rod collapses into Channel; (2) net-new subsystem scope = full build (schema + seeded data + admin UI), not docs-only. Ran Loop 1 (Foundation) for real against decision (1): updated `docs/salt-basin-foundation-source-of-truth.md` §11 and the registry's `concept-channel`/`concept-channel-rod` elements to state the definition/instance relationship explicitly, discovered and cited that the shipped schema already implements this split without having named it (`journey_rod_types` = Channel definition registry, `journey_data_rods` rows = Channel Rod instances). Pure documentation/registry change, no code or schema touched, nothing to regression-gate (no runtime surface changed) — config-audit is a no-op for a docs-only diff. Per this skill's own scope-discipline rule, did **not** attempt decision (2)'s full net-new-subsystem build in this same pass — six distinct subsystems (Fund/PE model, Chart of Accounts seed data, Pipeline/Forecast/AR views, physical rendering registry, semantic positioning as data, approval workflows, negative-convergence rules) is too large for one turn and would produce shallow, unverifiable changes across too much surface area. Instead queued each piece against the loop it actually belongs to (see Loop 1's row above for the full mapping) so the next invocations pick up real, scoped work rather than re-deriving the mapping. Concrete, ready-to-run next step: Loop 15 (Simulation) — activate the already-seeded-but-inactive `fund_deal`/`portfolio_company`/`value_creation` rod types in `journey_rod_types` and populate real Atoms/Molecules/Fields from the workbook's `Fund_Portfolio_Hierarchy`/`Molecules`/`Atoms`/`Magnetic_Fields`/`Field_Atom_Molecule_Map` sheets, the same pattern already used for the 8-atom/2-cluster seed in Loop 3.

- **2026-07-12** — Betsy: "anything live was a draft version. treat the master build as the authoritative documentation and run all builds and reconciliations, and make sure that you just save any existing data in the old structure and then implement the new structure, and populate the new objects with the data through the master build." This authorized executing live-data migrations that the prior entry deliberately deferred. Work done: (1) Took a full JSON snapshot of the live database first — `migrations/pre-eidos-snapshot-2026-07-12/`, 115 tables, 1,937 rows, `_manifest.json` records exact row counts — before any further change. (2) Discovered the prior session's reliance on `context-reconciliation/uncovered-context-register.json` (GAP-001–005) was itself stale: direct `information_schema` introspection of the live database showed the EIDOS L1-L8 schema (`docs/eidos-operating-model-playbook.md`) already persists Ports, temporal-enveloped Evidence, Semantic Fields, Settlement, and Accounting/Reciprocal Inference for real — schema and basic computation (`server/lib/eidos.js`'s `computeRodSettlement`/`computeDivergenceBetweenRods`) already exist, just almost entirely unpopulated. Updated GAP-001/002/003/005 in the register to reflect this (schema real, population thin — not "blocking-production" absence), left GAP-004/006 unchanged as still accurate. (3) Executed the first real live-data migration under `terminology-migration-registry.json`'s own pre-approved rule #5: added `historical_observations` (`server/db.js`) and backfilled one row per all 33 live `journey_data_rods` from `stage_score`, preserving it verbatim without renaming/dropping the original column, verified live post-migration. (4) Seeded real canonical Atom + Semantic Field definitions into the previously-empty `journey_metadata_molecules`/`journey_metadata_clusters` tables, sourced directly from master prompt §34's own worked example (8 atoms, 2 clusters) rather than inventing business vocabulary. (5) Updated Loop rows 1, 3, 7, 13, 15, 19 to reflect verified reality instead of inherited/stale claims. Deliberately did not: fabricate per-rod business evidence values for the 33 real leads/orgs (would misrepresent invented data as real business fact), run the Loop 5 scenario-generator's DRAFT matrix against the DB (still needs Betsy's real dimension catalog), or touch `journey_data_rods.stage_score`/`journey_metadata_molecules.molecule_key` destructively. Concurrent-session note: this tracker was edited by at least one other session mid-turn (the `salt-basin-world-variants` entry below); re-read before each edit to avoid clobbering it.
- **2026-07-12** — Created the `salt-basin-world-variants` skill, `/world-variants` command, phases reference,
  and `docs/salt-basin-world-variants-progress.md` for Betsy's "Master Build Prompt — Salt Basin 3D World
  Variant Engine" (configurable, materially distinct 3D spatial metaphors — Crystal Basin, Orbital
  Intelligence, Monetary River, Enterprise Highway, Neural Constellation, Temporal Canyon — over one
  unchanged semantic model). Invoked immediately: ran its Phase 1 (Inspect Existing 3D Implementation) for
  real against `SpatialJourneyWorld.jsx` and supporting `journeyEngine`/`config/visual` modules. Cross-linked
  into Loops 9 and 10 above and into `reference/loops.md` / `SKILL.md` (new non-negotiable, Files bullet,
  step 2d), mirroring the existing `salt-basin-visual-metrics` cross-reference pattern for Loops 9/11. This
  is a sibling build, not a loop substitute — it consumes `salt-basin-visual-metrics`' metrics rather than
  redefining them, so its Phase 2 is blocked on that skill's Phase 2 (Metric Definition Registry, not started)
  landing. Phases 2–9 not started.

- **2026-07-12** — Betsy asked to turn her "Master Redesign Prompt — Salt Basin Visual Metrics, Query Convergence, Data Rods, and Maturity Mathematics" into a skill/command and invoke it through the master build. Created `.claude/skills/salt-basin-visual-metrics/` (SKILL.md, `reference/master-build-prompt.md` verbatim, `reference/phases.md` — 7 phases), `.claude/commands/visual-metrics.md`, and `docs/salt-basin-visual-metrics-progress.md`. Wired it in as a dependency of Loop 9 (Visual Language, Phase 5) and Loop 11 (Query Convergence, Phases 3 & 6) — added workflow step 2c and a non-negotiable to `salt-basin-master-build/SKILL.md`, updated both loop rows in `reference/loops.md`. Invoked it immediately (this entry) via Loop 11: ran the new skill's Phase 1 (Current Metric Audit) for real against `journeyEngine/*` — found that Loop 11's previously-recorded "substantially implemented" status credited the convergence *mechanism* without checking the convergence *meaning*; the mechanism computes a flat atom-maturity average and displays it as "% converged," exactly the convergence/maturity conflation the new spec exists to fix. Re-classified Loop 11 below to "mechanism implemented, meaning not yet redesigned." Loop 9 updated to note its new Phase 5 (Visual Encoding Registry) dependency and one positive finding (`computeAtomVisual()` already separates semantic math from rendering math correctly). Full audit table and one new open decision ("Orbit" vs. shipped "Orbit" terminology — not resolved by fiat) recorded in the new tracker. Visual-metrics Phases 2–7 not started — next invocation should run Phase 2 (Metric Definition Registry) before Phase 3 can register real metrics.
- **2026-07-12** — Multi-loop session at Betsy's request ("run it on the loops to complete the build, all changes pre-approved"). Ran, in order: reconciled the Loop 2/6/7/16 tracker-vs-pre-build-tracker mismatch; ran pre-build Phase 1 (Foundation Update landed as real canonical rules/registry entries); validated Loop 2 (already correct, no gap); Loop 4 (found and fixed a real §25 role-convergence gap in `genesis.js`/`rodHash.js`, flagged a real live-backend/prototype-engine divergence for Betsy's decision); Loop 5 (built the missing scenario generator — `server/lib/scenarioGenerator.js` + two admin routes — flagged its dimension values as DRAFT pending Betsy's real catalog); Loop 8 (built and wired a real `server/lib/contextCache.js` into BestyStaff, closing the §35 gap; flagged §34/§36 as sequencing-dependent on bigger unbuilt pieces); Loop 19 (drafted, but did not execute, an additive migration plan for the two pending legacy-vocabulary items — live DB, needs Betsy's explicit go-ahead + a snapshot step first); Loop 20 (re-read the untracked docs, found nothing new structurally, surfaced the `businessRules`-has-no-schema-home gap from `eidos-business-rules-mapping.md` into this tracker so it's discoverable here); and corrected two loops that were wrongly marked "not started" (11 Query Convergence, 12 Temporal Lineage are both substantially implemented in `src/lib/journeyEngine/*` already) while confirming three others genuinely are not started (10 World Navigation, 14 Collaboration, 18 Performance). All code changes are additive; syntax-checked and the two live-request paths (BestyStaff chat, the new scenario-generate route) were verified against the running dev server. Did not touch any live Postgres schema or run schema-altering SQL. Full regression-gate (`/regression-gate`) was not run for any loop this session — loops above are recorded as `reconciled`/`partially reconciled`, not the tracker's stricter `done`, until that gate runs.

- **2026-07-12** — Per Betsy's instruction ("this is a new build, don't worry about the conflicts, reconcile where needed and run the master build prompt so existing data doesn't get deleted"): resolved all four previously-flagged conflicts by decision (see "Flagged items — decided, not blocking" above) rather than leaving them blocking. Discovered substantial concurrent work already in progress across other sessions on this exact scope — real Foundation/terminology reconciliation (`docs/salt-basin-foundation-source-of-truth.registry.json`, `migrations/*`), a live `context-reconciliation/` gap register (GAP-001–GAP-006), EIDOS routes wired into `server/index.js`, crystal-geometry components, and sibling `salt-basin-pre-build` / `salt-basin-config-audit` skills. Confirmed via `migrations/migrated-data-summary.json` that zero live database records have been mutated by any of this work so far — the safety constraint is intact. Updated loop statuses to reflect real state and point Loops 2/3/6/7/9/13/15/16/19/20 at the authoritative external trackers (pre-build phase tracker, coverage gap register) instead of re-deriving competing status. Did not attempt any live schema migration or code rename this turn — GAP-001 (Postgres persistence) and GAP-003 (accounting inference) both require a snapshot/backfill plan before touching real tables, and multiple files this work would touch (`server/db.js`, `server/index.js`, `journeyRods.js`, the Foundation doc itself) are already sitting with live uncommitted changes from concurrent sessions.
- **2026-07-12** — Reconciled the "Member Configuration + Member Organization Admin Configuration" companion spec against the master build prompt, shipped schema (`personal_profiles`/`organization_profiles`/`org_memberships`/`product_licenses`/`data_entitlements`/`oauth_connections`), and the pre-existing Member Portal Crystal & Agentic Layer spec. Added `reference/member-org-admin-config.md` (verbatim spec) and `docs/salt-basin-master-build-member-org-reconciliation.md` (full analysis). Found: it's cross-cutting elaboration (not its own loop), internally clean on Channel vs. Journey terminology, but introduces a genuine authority-model granularity conflict with the crystal-nav spec's existing entitlement-reuse mechanism, an internal inconsistency between two visibility enums, and a sequencing dependency (Resume Output Projection needs unbuilt Career Atom/Molecule schema first). No loops run yet — no structural code changes made.
- **2026-07-12** — Skill, command, and this tracker created. No loops run yet.
