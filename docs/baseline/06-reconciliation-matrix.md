# Reconciliation Matrix

**Status: partially populated (2026-09-10).** The orbital-design package (`05-new-requirement-register.md`'s NEW-001–017) is still blocked pending its own read-aloud validation — those rows are not below. The resume-product package's NEW-018–031 have a first reconciliation pass below, done as Phase 1 of the `salt-basin-resume-product` skill, against the "Career Foundation Sourcing & Reconciliation" system found live in `server/` (see `04-technical-element-register.md`'s `TE-CAREER-*` rows, all added this same pass). This is a first pass, not exhaustive — see "Not yet mapped" at the bottom.

## Required row schema (for when reconciliation begins)

| Mapping ID | New requirement and source | Existing definition IDs | Technical element IDs | Relationship | Required actions | Dependencies | Decision or uncertainty | Acceptance test IDs |
|---|---|---|---|---|---|---|---|---|

- **Relationship** ∈ {retain, extend, modify, replace, split, merge, retire, new, conflicting, unresolved}
- Every mapping row requires an explicit action, including "retain; no implementation change" where that's the true answer.
- Actions must name the affected layer: terminology, documentation, UI, interaction, data model, database migration, business rules, permissions, API, configuration, or tests.
- **Unmapped new requirements** and **existing features with no counterpart in the new documents** are both tracked here once they exist — silence in one direction never means deletion in the other.

## Resume-product package (2026-09-10 intake) — first reconciliation pass

| Mapping ID | New requirement and source | Existing definition IDs | Technical element IDs | Relationship | Required actions | Dependencies | Decision or uncertainty | Acceptance test IDs |
|---|---|---|---|---|---|---|---|---|
| RECON-001 | NEW-028 — Career Master Foundation as the single reusable source; career facts originate only with the user (`SRC-RESUME-11` §2–3) | — | TE-CAREER-01, TE-CAREER-02, TE-CAREER-03, TE-CAREER-04, TE-CAREER-05, TE-CAREER-12 | **extend** — a real Career Master Foundation already exists, just shaped as 7 legacy tables + a table-driven Atom mapping, not the spec's proposed Person/Employment/Achievement/Skill/Education/Project/Source/Claim/Target-role/Translation/Output-version record set | Documentation/data-model: decide whether the existing legacy-table+Atom shape is extended (add the record types with no current analog — see RECON-010) or whether spec §3's shape replaces it. Do not build a parallel schema without this decision | None yet — Betsy's decision | **Real, unresolved** — architecture-level choice, not a bug. Recommend extend, not replace, given how much already depends on the Atom shape (career_reasoning_*, career_opportunity_target scoring) | none yet |
| RECON-002 | Evidence-state vocabulary: user-attested / document-supported / externally verified / AI-proposed interpretation / disputed (`SRC-RESUME-11` §3, `SRC-RESUME-02`) | NEW-020 | TE-CAREER-03 (`source_truth_status`), TE-CAREER-05, TE-CAREER-06 | **partial overlap, modify** — existing vocabulary (`source_of_truth`/`primary_validated`/`user_attested`/`synthetic_scenario`) covers user-attested and (loosely) document-supported, but has no `externally verified` state and represents "disputed" structurally (an open `career_reconciliation_tasks` row), not as a claim-level flag | Data model: map the two vocabularies field-by-field before adding/renaming any column; decide whether "disputed" stays task-shaped (retain) or also needs a claim-level flag | RECON-001's shape decision | **Real, unresolved** | none yet |
| RECON-003 | User testimony alone is acceptable primary source; no document required below the review threshold (`SRC-RESUME-02`) | NEW-020 | TE-CAREER-03 | **retain** — the existing `user_attested` status already permits this exactly as specified | None — already correct | — | Resolved: already built | none yet |
| RECON-004 | Stretch-role application path: explicit missing-qualification acknowledgement, no invented transferable skill (`SRC-RESUME-02`) | NEW-021 | — | **new** — no existing analog found in this pass | UI/journey: net-new acknowledgement flow, likely at resume-output or application-stage time (TE-CAREER-09's `applied` transition is the natural attachment point) | TE-CAREER-09 | Not yet designed | none yet |
| RECON-005 | Three scoring/threshold concepts (DEC-006): opportunity ranking, requirement-level transfer coverage (75/50), review-gating bands (90/75, NEW-019) | NEW-019 | TE-CAREER-10 (opportunity ranking — **confirmed identical** to S03 §7, see its Evidence column) | **retain** (opportunity ranking) + **new** (the other two) | UI: never render TE-CAREER-10's score as if it were a review-gating threshold or a requirement-transfer percentage. Build the 75/50 requirement-coverage scorer and the 90/75 review-gating bands as their own, separately-labeled mechanisms | TE-CAREER-09, TE-CAREER-10 | DEC-006 substantially answered by this audit (three concepts, confirmed distinct); which two new ones get built and how they're labeled in UI is still Betsy's call | none yet |
| RECON-006 | "No recurring LLM cost once context/memory is cached" (DEC-009) | — | TE-CAREER-08 | **extend** — `careerReasoningCompiler.js`'s admin-approved, population-denominator-based reusable reasoning-pattern cache is a real, working instance of exactly this pattern for one narrow case (resolving a reconciliation task's reasoning) | Architecture: use this same admin-approval + eligible-population-denominator pattern as the model for DEC-009's broader deterministic-vs-cached-LLM-vs-recurring-LLM classification, rather than inventing a new caching mechanism | RECON-005 | DEC-009 still needs its full D01–D11 classification; this narrows the "how" once that classification happens | none yet |
| RECON-007 | Document generation ≠ having applied (NEW-026) | NEW-026 | TE-CAREER-09 (`ALLOWED_TRANSITIONS`) | **retain** — `applied` is already a distinct, validated stage transition, never inferred from output generation | None — already correct | — | Resolved: already built | none yet |
| RECON-008 | Calendar integration, interview tracking, outcome→material linkage (NEW-027) | NEW-027 | TE-CAREER-09 (`interviewing`/`offer`/`rejected`/`withdrawn` stages exist) | **partial — extend** — stage tracking exists; no calendar-provider integration (Google/Outlook) or outcome→specific-wording linkage found anywhere in this pass | API/data model: calendar OAuth + event linkage is net-new; scope separately per this skill's own phases.md note (Phase 6) | TE-CAREER-09, the OAuth provider system (`server/lib/oauthProviders.js`) | Not yet designed; likely large enough to split further | none yet |
| RECON-009 | Personal vs. shared learning, explicit member opt-in, minimum group size before shared findings apply (NEW-022, NEW-023) | NEW-022, NEW-023 | TE-CAREER-08 | **conflicting — flag for Betsy** | Product decision: `careerReasoningCompiler.js`'s cross-user pool is fed by *any* resolved task with `reasoningApproved: true` — there is no per-member opt-in gate today, only admin curation after the fact. `SRC-RESUME-03`/UXT-03 wants shared contribution to be a separate, explicit **member**-level opt-in. These are two different consent models (admin-gated vs. member-gated) | TE-CAREER-06, TE-CAREER-07, TE-CAREER-08 | **Real, unresolved** — does admin curation satisfy Betsy's intent, or does a member-facing opt-in need to sit in front of the existing pipeline? Her call | none yet |
| RECON-010 | LinkedIn/reference disclosure controls: direct / upon-request / omit, per link (NEW-024, NEW-025) | NEW-024, NEW-025 | — (not confirmed absent — `personal_profiles`/`member_configs` not checked this pass) | **unmapped — needs a targeted check** | Before marking "new," check `personal_profiles` and `member_configs` schemas for any existing link-visibility field | — | Open — this row is honestly incomplete, not a confirmed gap | none yet |
| RECON-011 | Evidence-linked recommendations, approvable at wording/sentence/paragraph/recommendation granularity (NEW-018) | NEW-018 | TE-CAREER-06, TE-CAREER-07 | **partial overlap, extend** — approval/reasoning exists at the Career-Master-field level (a reconciliation task), nothing found yet at the finished-resume-output wording/sentence/paragraph level, which is a different layer (rendering, not intake) | UI/data model: net-new granular approval layer over rendered resume output, reusing TE-CAREER-06/07's approval-record shape as its model rather than inventing a new one | TE-CAREER-06, TE-CAREER-07, RECON-001 | Not yet designed | none yet |
| RECON-012 | Below-threshold prompt for user's own transfer rationale, retained as reusable Career Master testimony (NEW-029) | NEW-029 | TE-CAREER-06 (`ambiguous_mapping` detection + `reasoning` capture) | **partial overlap, extend** — the detection/capture mechanism exists; the exact suggested collection prompt copy from `SRC-RESUME-11` §5 is not confirmed wired into `CareerReconciliationPanel.jsx`'s actual UI text | UI copy: confirm/update prompt wording against the existing panel | TE-CAREER-06, TE-CAREER-11 | Minor — likely a copy change once confirmed, not new mechanism | none yet |
| RECON-013 | Dual factual-provenance / editorial-contribution ledger (NEW-030) | NEW-030 | TE-CAREER-07 | **partial overlap, extend** — `career_reasoning_approvals` records who approved what reasoning and when, but lacks the fuller transformation-type taxonomy (verbatim/extraction/paraphrase/translation/etc.) and previous-vs-proposed-text pairing `SRC-RESUME-11` §7 specifies | Data model: extend `career_reasoning_approvals` (or a sibling table) with transformation-type and before/after text fields rather than a parallel ledger | TE-CAREER-07 | Real but scoped — an additive column/table extension, not a new system | none yet |

## Not yet mapped (honest gap, not a claim of completeness)

NEW-031 (resume must describe this product truthfully at its actual stage) has no existing technical element to reconcile against — it's a content/copy rule for whatever resume-output renderer eventually exists, not a data-model question. DEC-007/008/009's platform-wide asks (free-trial gating, external-agent API, self-hosted build access) are deliberately not reconciled here — see `07-decision-log.md`, they are out of this subsystem's scope. `TE-CAREER-11`'s panels have not been exercised in a live browser this session (no DB credentials available) — every "retain, already correct" row above is a **code-level** finding, not a verified-in-the-running-app finding; per this program's own rule, that distinction stays visible until Stage 5 verification actually happens.
