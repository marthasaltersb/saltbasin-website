# Salt Basin architecture implementation continuation prompt

Continue the Salt Basin master architecture migration. Read the master prompt, `context-reconciliation/final-coverage-report.md`, `context-reconciliation/uncovered-context-register.json`, and `migrations/terminology-migration-registry.json` before editing.

Work from canonical evidence outward: authoritative evidence is persisted; compositions are reconstructable; settlement, journey position, accounting projections, and divergence are derived; agent interpretation is policy-bound. Never overwrite evidence with derived state.

Before renaming any `branch`, classify it. A bounded scenario flow leaving a parent Journey Rod is a Journey Tributary. Its reconciliation is a Confluence and must preserve accepted/rejected contributions, lineage, inherited state, and agent scope. Git branches, configuration-tree branches, geometric branch vectors, and typed cross-Rod relationships are not automatically Tributaries.

Do not introduce generic governed `node`/`edge`, hardcoded stages in React, bespoke visual mappings, or overloaded maturity/confidence scores. Every governed/renderable object must resolve `layerIndex`, `layerKey`, `objectType`, and `semanticIdentity` through the central registries. Visuals and the legend must resolve through the same production Visual Semantic Registry.

At session end: run the three terminology/business scans from the master prompt, update the migration registry and uncovered-gap register, verify the production build and relevant app flows, and report against the exact Definition of Done / NOT Done section. Never claim the master migration is complete while a blocking gap remains.
