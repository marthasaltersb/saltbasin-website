# Salt Basin Pre Build — Progress Tracker

Mutable state for the `salt-basin-pre-build` skill. Read first, update last, on every invocation. Static phase definitions live in `.claude/skills/salt-basin-pre-build/reference/phases.md` — don't duplicate that content here, only status/findings/changelog.

## Open decisions (answer here when Betsy decides; skill must not default on these)

| # | Decision | Status | Answer |
|---|----------|--------|--------|
| 1 | `authorityProfileIds`/`moduleAccessProfileIds`/`viewAccessProfileIds` three-array split vs. extending `data_entitlements.scope` | answered (2026-07-12, recorded in master-build tracker, previously not synced here) | Extend `data_entitlements.scope`. No new profile tables unless a concrete module needs gating the JSONB shape can't express. |
| 2 | Reconcile the two visibility enums (§3 object-level vs. §15 record-level Data Scope) — one taxonomy at two granularities, or two separate concerns? | answered (2026-07-12, recorded in master-build tracker, previously not synced here) | One canonical six-value Data Scope enum (`MEMBER_PRIVATE/MEMBER_PUBLIC/ORGANIZATION_PRIVATE/ORGANIZATION_SHARED/CROSS_ORGANIZATION_AUTHORIZED/PUBLIC`) for both record- and object-level visibility; §3's five-value enum is retired as UI-copy shorthand mapping onto it, not a second real enum. |
| 3 | Add Member-scoped Channel Rods to the Foundation registry now, or hold until Phase 4 financial infrastructure exists? | unanswered | — |

## Phase status

| # | Phase | Status | Last run | Notes |
|---|-------|--------|----------|-------|
| 1 | Canonical Identity & Foundation Update | done | 2026-07-12 | Foundation Update (§20) landed: `docs/salt-basin-foundation-source-of-truth.md` (Member Organization Admin identity rule + baseline personal-config capabilities, under §11) and `.registry.json` (`rule-member-org-admin-identity`, `rule-member-financial-privacy` canonical rules, `concept-member-identity` element, registryVersion bumped to `.channel-3`). No schema change needed — decision #1 (below) confirms `data_entitlements.scope` extension over new profile tables, and no concrete module yet needs finer gating, so no premature tables were added. |
| 2 | Personal Brand Website & World | in progress | 2026-08-07 | First real slice landed: §2's Site Configuration surface (identity, theme, brand colors, social links, tagline, public/private/draft state) is now a real World Shell island for both Betsy and members — `src/components/WorldShell.jsx`'s `SiteConfigView` (new `'embed'` island kind, full-screen since `ConfigPanel.jsx` is 1600+ lines, too big for the docked rail) wraps the existing `ConfigPanel.jsx` wholesale (reused, not forked), self-contained load/save/publish per role. `defaultMemberConfig.js`'s `memberTabs` gained a `config` entry (re-enabling what was explicitly disabled 2026-07-30); `AdminShell.jsx`'s member gate on the inline 'config' fallback branch removed. Admin's island source broadened from just `admin_nav`'s `crm` view to *all* views flattened (`resolveWorldIslands` already silently skips unregistered componentIds, so this is safe) — surfaced that Betsy's own `config` tab lives under the `system` view, not `content` as assumed going in. Verified end-to-end against local Postgres: both roles see the island, open the real ConfigPanel, edit a field, Save Draft persists to the DB (confirmed via direct query), Publish call wired. **Not yet done, explicitly deferred to a later pass of this phase**: §3's Personal Brand World (spatially representing Career Channel Rod/Skill Molecules/Case Study Molecules/Resume Outputs as selectable world objects with the six-value Data Scope enum) — this pass only shipped the *Website* (§2) half, not the *World* (§3) half. That's the next slice. |
| 3 | Resume Configuration & Output Projection | unblocked (was blocked) | 2026-07-28 | Re-checked against current schema per this skill's own "phase is not done permanently, re-verify" rule: the reconciliation memo's blocker ("assumes a versioned Career Atom/Molecule model that doesn't exist yet") predates 2026-07-16's real Career Channel Rod build (`journey_data_rods` rod_type='career_master', `careerAtomRegistry.js`, `careerAtomMigration.js`, `careerAtomRollups.js` — documented in CLAUDE.md's "Career Channel Rod" section, live-synced from the legacy `career_*` CRUD tables on every create/update/delete). The Atom/Molecule model §4–§7 needed now exists for real. Not yet built: the `ResumeOutputProjection` object itself against this data (a separate `resume_output_projections` table already exists per CLAUDE.md but is described there as having "no automated trigger populating it for most members" — a known, separate gap) and the public-site/resume UI overlap Betsy asked for 2026-07-28. Next actual build step, not yet started. |
| 4 | Financial Connections & Personal Financial Channels | in progress | 2026-07-28 | Nav-visibility decision landed 2026-07-28: `financial` tab removed from `defaultMemberConfig.js`'s default member nav (and patched onto the existing test member/org config rows) since there's no member-facing UI worth shipping yet — `MemberFinancialPanel` and its routes are untouched, just unlinked from nav. Real backend work (Plaid Link, refresh ingestion, semantic Atom population) still open per the 2026-07-12 entries below. |
| 5 | Member Financial/Brand/Resume Staff | not started | — | No open questions, but depends on Phase 2/4 surfaces existing to attach to |
| 6 | Org Admin Modules, Data Scope, Worlds, Access Resolution, Context Switching | in progress | 2026-07-28 | Decision #2 now resolved (see above) — only decision #3 (Member-scoped Channel Rod registry timing) remains a real blocker, and that specifically concerns the Foundation §17 Channel Rod list, not this phase's org-admin-module/world scope. Verified directly against the live database that substantial real infrastructure already exists, previously unrecorded here: `org_sites`/`org_configs` (draft/published pair, same pattern as `member_sites`/`member_configs`) and `server/routes/orgPortal.js` (real, working read/write/publish CRUD for both, org-membership-gated access with `canEdit` derived from `org_memberships.role==='admin'`) plus `AdminShell.jsx` already handling `org-admin`/`org-user` scopes. This is real Worlds + Access Resolution groundwork for this phase, done in an earlier (already-committed, `9eda2b7 Add organization portal scopes`) piece of work this tracker hadn't credited. Re-classified from "blocked" — Data Scope enum application to these tables and full module/context-switching UI still need building, but the phase is no longer stalled on an open question. **2026-07-28**: confirmed a real, concrete gap in this phase's scope — there is currently NO way for an org-admin to provision/invite another user into their org (`org_memberships` rows are only ever created today via the work-email domain-match trigger or lead→org promotion, never by direct org-admin action). Betsy asked for this 2026-07-28; not yet built, next candidate slice for this phase. Also surfaced: the member-facing "Platform" nav tab (`memberPlm` → `MemberPlmPanel`) renders Betsy's own internal Salt Basin build/backlog-lifecycle tracker (`platformLifecycleConfig.js`, `backlogItemToLifecycleRecord`) — almost certainly copy-pasted from the admin nav template rather than a real member-facing concept; needs a Betsy decision (repurpose vs. remove), not defaulted here. |

## Gaps found 2026-07-28 that are NOT covered by the 20-section spec (flagging, not defaulting)

Betsy asked to "clean up the member home screen" — most of what she described maps onto existing phases (above), but two pieces genuinely aren't addressed anywhere in the source spec or its reconciliation memo, and need her direction before any code:

- **Messaging rules (who can message whom).** The "Messages" tab (`inbox` componentId) exists and is wired, but there is no defined policy for who a member can message, who can message a member, or how an org-organization relationship affects that. Not mentioned anywhere in the 20-section spec. Needs Betsy's actual rules before building gating logic — not something to infer from the entitlement model by analogy.
- ~~**First-login "orbit experience" + profile-function-scoped navigation.**~~ **Resolved, outside this skill's own workflow** — built 2026-08-07 in the same session (not as a pre-build phase run) as `src/components/WorldShell.jsx`: a real, literal 3D crystal-orbit landing at `/admin` and `/member` for both roles, islands resolved dynamically from `admin_nav`/`memberTabs`. Answers the open question this gap raised in favor of "literal 3D orbit." Recorded here so this tracker doesn't re-flag it as unaddressed on a future invocation — Phase 2's remaining work (below) now builds *on top of* WorldShell rather than needing to invent the shell itself.

## Changelog

- **2026-08-07** — Ran Phase 2 (Personal Brand Website & World) for the first time, at Betsy's request after she described a much larger member-world vision (site config, portfolio outputs, career placement services, career foundation intake redesign, BestyStaff scoping) and was directed here via the skill's own recommendation over freelancing a plan. Scoped to a single concrete, reviewable slice per this skill's scope-discipline rule rather than a shallow pass across everything she described: shipped §2 (Site Configuration) as a real, working World Shell island for both admin and member — see Phase 2 row for the structural detail. Also resolved the 2026-07-28 "first-login orbit experience" gap (see "Gaps found," now struck through) — it was independently built the same day, outside a pre-build invocation, as `WorldShell.jsx`. Explicitly NOT built this pass, sequenced as next slices: §3 Personal Brand World (spatial semantic-object representation + Data Scope visibility), Phase 3 (Resume Configuration & Output Projection — Betsy's "Portfolio Outputs config island"), Phase 5 (Member Financial/Brand/Resume Staff — Betsy's "BestyStaff popup scoped to site/career/placement"), and the career-foundation-intake-redesign + career-placement-services-extension material from Betsy's message, which doesn't map cleanly onto any of the 6 phases and needs its own scoping pass (largely because it's downstream of the existing, separately-tracked Career Placement Agents build from earlier this session, not this spec's §1–§20 directly).

- **2026-07-28** — Betsy asked to "clean up the member home screen" (orbit-style first-login experience, profile-function-scoped navigation, DTC member view definition, hide Financial from members, messaging rules, member org user provisioning, unclear Network/Platform/Documents scope) — triggered live while testing as a real converted member (Breck Golden). Landed the concrete, unblocked pieces this pass: removed `financial` from `defaultMemberConfig.js`'s default member nav and patched it off the existing test member/org config rows (Phase 4 row); re-verified and unblocked Phase 3 (the Career Channel Rod it was waiting on has existed since 2026-07-16); confirmed and recorded the member-org-provisioning gap and the `memberPlm`/"Platform" tab's likely-mis-scoped admin-tool leakage (Phase 6 row). Flagged messaging rules and the first-login nav redesign as real gaps outside the 20-section spec's scope — not defaulted, waiting on Betsy's direction (see "Gaps found" section above). Nothing else built this pass; scope discipline per this skill's own rules — six phases, don't shallow-pass all of them in one turn.

- **2026-07-12** — Phase 4 continuation added a Member Financial panel and connection revocation, then corrected an initial configuration-enforcement failure before handoff. Member navigation is now config-driven through `defaultMemberConfig.navigation.memberTabs`; financial provider enumeration, ordering, availability, labels, experience defaults/copy, account classes, liability classification, scopes, and derived share types resolve through `financialPolicyRegistry.js`. Existing member config rows are not mutated by bootstrap; the draft-config response additively supplies the default navigation when an older row has no navigation key.

- **2026-07-12** — Advanced Phase 4 (Financial Connections & Personal Financial Channels). Added `financial_consents`, `external_financial_connections`, `financial_account_definitions`, and `financial_output_shares`; raw connection/account rows are structurally `MEMBER_PRIVATE` and intentionally have no Organization scope. Added `financialPolicyRegistry.js` for provider classes, account/liability classification, security/retention IDs, canonical Data Scope, and permitted derived share outputs. Added `/api/member-financial` routes for Member-owned connection/account creation and explicit Organization-output share/revoke/read flows. Credentials/tokens are not accepted by these routes and do not become Evidence Atoms. Unit policy tests pass (4/4); production Vite build completed. Phase remains in progress because real Plaid Link/token-vault integration, source refresh, semantic Atom population, and interacting-user UI verification are not built yet.

- **2026-07-12** — Ran Phase 1 (Canonical Identity & Foundation Update). Synced open-decision #1 with the master-build tracker's already-recorded answer (extend `data_entitlements.scope`) rather than leaving this tracker showing it unanswered — the two trackers had diverged. Landed §20's Foundation Update as real canonical rules/elements in the Foundation doc and registry (see Phase 1 row). Phases 2/6 are now unblocked on the identity-model dependency (Phase 2 still needs its own scene-content build; Phase 5 still needs Phase 2/4 surfaces to attach to). Phases 3 and 6 remain blocked (Career Atom/Molecule model; open decisions #2/#3).
- **2026-07-12** — Created the `salt-basin-pre-build` command and skill at Betsy's request, using the "Member Configuration + Member Organization Admin Configuration" spec already ingested as a master-build companion document (`.claude/skills/salt-basin-master-build/reference/member-org-admin-config.md`, added earlier the same day) and its reconciliation memo (`docs/salt-basin-master-build-member-org-reconciliation.md`). No phases run yet, no structural code changes made — this entry only records the skill/command/tracker scaffolding.
